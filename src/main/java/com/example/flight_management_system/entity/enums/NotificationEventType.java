package com.example.flight_management_system.entity.enums;

public enum NotificationEventType {
    FLIGHT_DELAYED,
    FLIGHT_GATE_CHANGED,
    FLIGHT_CANCELLED,
    BOOKING_CANCELLED,
    BOOKING_REBOOKED,
    BOOKING_CONFIRMED,
    WAITLIST_PROMOTED
}
